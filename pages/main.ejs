<% include header %>

<% if(sess.email){ %>
<div class="jumbotron">
    <h2>Bienvenue <%= sess.firstname + ' ' + sess.name%> </h2>

    <div class="row">
        <div>
            <h3 style="float: left;">Mes dessins</h3>
            <h2><a style="float: right" class="btn btn-primary" role="button" href="paint"> Créer un dessin >></a></h2>
        </div>
    </div>
    <div class="table-responsive">
        <table class="table text-center">
            <thead>
            <tr>
                <td>Nom du dessin</td>
                <td>Voir</td>
            </tr>
            </thead>
            <tbody>
            <% if(rows.lenght==0){ %>
            <tr>
                <td colspan="3">Pas de dessin</td>
            </tr>
            <%} else{%>
            <% for (var i = 0; i < rows.length; i++) { %>
            <tr>
            <td><%= rows[i].name %></td>
            <td><a href="guess/<%= rows[i].id_draw %>" class="btn btn-primary"><i class="glyphicon glyphicon-eye-open"></i></a></td>
            </tr>
            <% }%>

            <%}%>
            </tbody>
        </table>
    </div>
</div>
<% } else {%>
<div class="jumbotron">
    <h2>Bienvenue sur le site Pictionnary en NodeJS</h2>

    <a class="btn btn-primary" role="button" href="login"> Connectez-vous >></a>
    <a class="btn btn-primary" role="button" href="register"> Inscrivez-vous >></a>
</div>
<% }%>

<% include footer %>